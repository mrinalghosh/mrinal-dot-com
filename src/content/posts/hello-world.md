---
title: "Hello, whiteboard"
description: "A first note on the new site."
pubDate: 2026-07-07
---

This is a placeholder post. To publish a new one, drop a Markdown file in
`src/content/posts/` with `title`, `description`, and `pubDate` in the
frontmatter — it shows up on the [writing](/writing) page automatically.

You can use all the usual Markdown: **bold**, *italics*, `code`, lists,
images, and code blocks.

```glsl
void main() {
  gl_FragColor = vec4(vec3(0.98), 1.0);
}
```

> The magnets are still up there, by the way. Drag them around.
